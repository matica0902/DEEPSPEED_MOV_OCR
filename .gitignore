# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv
*.egg-info
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Uploads (will be created at runtime)
uploads/*
!uploads/.gitkeep

# Model cache
.cache/
hf_cache/
/tmp/hf_cache/

# Environment variables
.env
.env.local

# Test files
test_*.py
*_test.py
check_gpu.py

# Railway/Docker
# .dockerignore 需要推送到 GitHub（Railway 需要）

# 不需要推送到 GitHub 的文件
# 文档和分析文件
*.md
!README.md

# macOS 专用文件
Dockerfile.macos

# Docker Compose（Railway 不需要）
docker-compose.yml

# 启动脚本（保留用于本地开发，Railway 会自动使用 Dockerfile CMD）
# start.sh 和 startback.sh 已保留在仓库中供本地使用

# 备用模板和静态文件
templates/indexback*.html
static/appback*.js
